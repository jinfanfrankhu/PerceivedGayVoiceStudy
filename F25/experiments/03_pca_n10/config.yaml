name: 03_pca_n10
note: "PCA n_components=10, SVC linear C=0.1 balanced"

model:
  type: svc
  kernel: linear
  C: 0.1
  class_weight: balanced

feature_selection:
  method: pca
  n_components: 10

labels: [binary, three_class, ordinal]
